import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  root: {},
  icon: {
    marginRight: 8,
  },
  subText: {
    color: "rgba(0,0,0,.54)",
  },
  totalText: {
    fontWeight: "bold",
    color: "rgba(0,0,0,.54)",
    fontSize: 18,
  },
}));
