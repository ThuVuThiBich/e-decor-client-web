import {
  Box,
  makeStyles,
  Paper,
  Step,
  StepConnector,
  StepLabel,
  Stepper,
  withStyles,
} from "@material-ui/core";
import LocalShippingIcon from "@material-ui/icons/LocalShipping";
import clsx from "clsx";
import React, { useState } from "react";

export default function Product() {
  return <Paper>Product</Paper>;
}
